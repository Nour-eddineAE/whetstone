---
id: p25
category: cte
difficulty: med
tracks: sql, pyspark
ordered: false
columns: dept, total_salary, rnk
tables: employees
hint: First aggregate total salary per dept (CTE / a grouped DataFrame), then RANK over that and keep the top 3.
hint_pyspark: groupBy("dept").agg(F.sum("salary")) then F.rank().over(Window.orderBy(total desc)); keep rnk <= 3.
---
**Result:** the top 3 departments by total salary, each with its rank (`rnk` = 1 for the highest total).
