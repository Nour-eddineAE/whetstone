---
id: p12
category: patterns
difficulty: med
tracks: sql, pyspark
ordered: false
columns: second_highest
tables: employees
hint: MAX(salary) WHERE salary < (SELECT MAX(salary)). Handles ties for the top automatically.
hint_pyspark: Distinct salaries, then F.dense_rank().over(Window.orderBy(F.col("salary").desc())) == 2.
---
**Result:** the second-highest distinct salary in the company, as a single value.
