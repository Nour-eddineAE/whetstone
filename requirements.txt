# Pinned to the versions this project is developed and tested against
# (Python 3.11). PySpark also needs a JDK on the PATH (e.g. openjdk@17).
duckdb==1.5.4
pyspark==4.1.2
pandas==3.0.3
flask==3.1.3
markdown==3.10.2
jedi==0.20.0
