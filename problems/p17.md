---
id: p17
category: sparkops
difficulty: med
tracks: sql, pyspark
ordered: false
columns: band, num_employees
tables: employees
hint: CASE WHEN / when().otherwise() to bucket salary, then GROUP BY the band and count.
hint_pyspark: F.when(...).when(...).otherwise(...) to bucket salary into a band column, then groupBy(band).count().
---
**Result:** how many employees fall into each salary band:

- `low` - salary under 60000
- `mid` - salary 60000 to 89999
- `high` - salary 90000 and above
