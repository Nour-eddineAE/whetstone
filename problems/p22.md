---
id: p22
category: joins
difficulty: med
tracks: sql, pyspark
ordered: false
columns: emp_id, name, salary, manager_salary
tables: employees
hint: Self-join employees to employees on e.manager_id = m.emp_id, then keep e.salary > m.salary.
---
**Result:** the employees who earn more than their own manager.

Show the employee's `emp_id`, `name`, `salary`, and their manager's salary as `manager_salary`.
