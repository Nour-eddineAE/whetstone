---
id: p19
category: sparkops
difficulty: med
tracks: sql, pyspark
ordered: false
columns: total, distinct_all, distinct_user_event
tables: events
hint: distinct() dedups whole rows; dropDuplicates(subset) dedups on chosen cols. Return one row of 3 counts.
hint_pyspark: df.distinct().count() for whole rows, df.dropDuplicates([cols]).count() for a subset; assemble one row of three counts.
---
**Result:** a single row with three counts over the events table:

- `total` - how many rows there are
- `distinct_all` - how many distinct rows (every column considered)
- `distinct_user_event` - how many distinct `(user_id, event_type)` combinations
