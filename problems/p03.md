---
id: p03
category: joins
difficulty: med
tracks: sql, pyspark
ordered: false
columns: emp_id, name
tables: employees
hint: NOT IN (subquery with NULLs) returns NOTHING. Use NOT EXISTS or LEFT ANTI JOIN instead.
hint_pyspark: Left-anti join employees against the distinct manager_id values: e.join(mgr_ids, e.emp_id == col("manager_id"), how="left_anti").
---
**Result:** the employees that nobody reports to - those whose `emp_id` is never listed as another employee's `manager_id`.
