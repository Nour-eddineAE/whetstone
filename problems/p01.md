---
id: p01
category: joins
difficulty: easy
tracks: sql, pyspark
ordered: false
columns: emp_id, name, dept_id
tables: employees, departments
hint: INNER JOIN drops rows with no match on EITHER side; join employees.dept to departments.dept_name.
hint_pyspark: Inner join (how="inner", the default): e.join(d, e.dept == d.dept_name) then .select the columns.
---
**Result:** each employee together with the id of their department.

Employees whose department isn't listed in the departments table (e.g. `Operations`) are left out.
