---
id: p23
category: joins
difficulty: med
tracks: sql, pyspark
ordered: false
columns: emp_id1, emp_id2, dept
tables: employees
hint: Self-join on same dept with a.emp_id < b.emp_id to avoid self-pairs and mirrored duplicates.
---
**Result:** every pair of distinct employees who work in the same department, each pair listed once.

Output the two ids as `emp_id1` and `emp_id2` (with `emp_id1 < emp_id2`) and their `dept`.
