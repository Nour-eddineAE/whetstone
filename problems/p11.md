---
id: p11
category: window
difficulty: hard
tracks: sql, pyspark
ordered: false
columns: user_id, txn_id, txn_date, running_total, grand_total
tables: transactions
hint: ORDER BY in the window => running frame. No ORDER BY (just PARTITION BY) => whole-partition total.
hint_pyspark: Two windows on user_id: with .orderBy => running sum; partitionBy only (no orderBy) => whole-partition total.
---
**Result:** every transaction showing two numbers for its user:

- `running_total` - the cumulative amount up to and including this transaction (in `txn_date`, then `txn_id` order)
- `grand_total` - the user's overall total across all their transactions
