---
id: p15
category: patterns
difficulty: hard
tracks: sql, pyspark
ordered: true
columns: event_type, users
tables: events
hint: COUNT(DISTINCT user_id) per step; order the rows view -> signup -> purchase with a CASE/mapping.
hint_pyspark: Filter to the 3 event types, groupBy("event_type").agg(F.countDistinct("user_id")), order with an F.when mapping view->signup->purchase.
---
**Result:** how many distinct users reached each funnel step - `view`, `signup`, `purchase` - listed in that order.

Other event types are ignored.
