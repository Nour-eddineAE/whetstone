---
id: p24
category: aggregation
difficulty: med
tracks: sql, pyspark
ordered: false
columns: dept, headcount, avg_salary
tables: employees
hint: GROUP BY dept then HAVING on the aggregates (avg > 70000 AND count > 5). HAVING filters groups, WHERE filters rows.
---
**Result:** the departments that have **more than 5** employees **and** an average salary **above 70000**.

Show each such department's `headcount` and `avg_salary`.
