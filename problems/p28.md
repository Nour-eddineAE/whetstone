---
id: p28
category: sparkops
difficulty: med
tracks: sql, pyspark
ordered: false
columns: user_id, view, signup, purchase, click, login
tables: events
hint: Spark groupBy('user_id').pivot('event_type', [...]).count().na.fill(0). SQL - SUM(CASE WHEN event_type=...). 
hint_pyspark: groupBy("user_id").pivot("event_type", ["view","signup","purchase","click","login"]).count().na.fill(0).
---
**Result:** per user, a count of each event type as its own column - `view`, `signup`, `purchase`, `click`, `login`.

A user with none of a given type shows `0` for it.
