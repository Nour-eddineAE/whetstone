---
id: p07
category: window
difficulty: med
tracks: sql, pyspark
ordered: false
columns: emp_id, dept, salary, rnk, dense_rnk
tables: employees
hint: RANK leaves gaps after ties (1,1,3); DENSE_RANK does not (1,1,2). Same PARTITION/ORDER for both.
hint_pyspark: Same window (partitionBy dept, orderBy salary desc) for both: F.rank().over(w) leaves gaps, F.dense_rank().over(w) does not.
---
**Result:** every employee with two salary rankings inside their department (highest paid = 1):

- `rnk` - tied employees share a number and the next rank skips ahead (1, 1, 3)
- `dense_rnk` - tied employees share a number but the next rank does not skip (1, 1, 2)
