---
id: p16
category: patterns
difficulty: hard
tracks: sql, pyspark
ordered: false
columns: user_id, median_amount
tables: transactions
hint: DuckDB has median(); Spark use expr('percentile(amount, 0.5)') for an exact interpolated median.
---
**Result:** the median transaction amount for each user.

With an even number of transactions, the median is the average of the two middle amounts.
