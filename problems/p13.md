---
id: p13
category: patterns
difficulty: med
tracks: sql, pyspark
ordered: false
columns: user_id, event_type, event_ts
tables: events
hint: Keep-latest = MAX(event_ts) per (user_id, event_type), or ROW_NUMBER ordered by ts DESC keeping rn=1.
hint_pyspark: Window.partitionBy("user_id","event_type").orderBy(F.col("event_ts").desc()); keep row_number == 1. Or groupBy(...).agg(F.max("event_ts")).
---
**Result:** for every user-and-event-type, only their latest occurrence - one row per pair, with the most recent timestamp.
