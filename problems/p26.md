---
id: p26
category: dates
difficulty: med
tracks: sql, pyspark
ordered: false
columns: month, txn_count, total_amount
tables: transactions
hint: Truncate/format txn_date to YYYY-MM (strftime / date_format), then group by that month.
---
**Result:** for each calendar month (`YYYY-MM`), how many transactions there were (`txn_count`) and their total amount (`total_amount`).
