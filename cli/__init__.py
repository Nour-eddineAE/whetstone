"""cli - command entrypoints, dispatched by the root manage.py.

Each module exposes main(args) and stays thin: parse args, call into pengine
(or webapp), print. No grading/business logic lives here.
"""
