---
id: p09
category: window
difficulty: med
tracks: sql, pyspark
ordered: false
columns: user_id, txn_id, txn_date, amount, running_total
tables: transactions
hint: SUM(amount) OVER (PARTITION BY user_id ORDER BY txn_date, txn_id ROWS UNBOUNDED PRECEDING).
---
**Result:** every transaction, plus a `running_total` - the user's cumulative amount up to and including that transaction.

Transactions accumulate in `txn_date` order (and by `txn_id` when the date ties).
