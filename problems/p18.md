---
id: p18
category: sparkops
difficulty: med
tracks: sql, pyspark
ordered: false
columns: user_id, tag
tables: user_tags
hint: Spark explode(split(tags, ',')); DuckDB unnest(string_split(tags, ',')). One row per tag.
hint_pyspark: F.explode(F.split("tags", ",")) gives one row per tag; alias the exploded column.
---
**Result:** one row per user-and-tag.

Each user's `tags` value is a single comma-separated string (e.g. `vip,beta`); turn it into a separate row for each tag.
